# Build output
build/
DerivedData/
*.xcuserdata
xcuserdata/

# Staged release-app inside bundle/server/ is recreated by every
# packaging run — don't check it in. The packaged .mcpb at repo root
# is checked in so end users can grab it without building.
bundle/server/

# macOS noise
.DS_Store

# Clang coverage artifacts
*.profraw
